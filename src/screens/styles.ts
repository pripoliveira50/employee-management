import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    backgroundColor: "#FFFFFF",
  },
  space: {
    marginVertical: 10,
  },
  label: {
    fontWeight: "bold",
    fontSize: 20,
  },
});
